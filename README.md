# AQC-Display

This project contains the code for a RP2040 with a TFT display which reads and displays air quality sensor data.

# Development

Use the following commands to build, flash and clean:

```Make
make build
make flash
make clean
```
